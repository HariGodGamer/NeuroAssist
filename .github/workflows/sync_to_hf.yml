name: Sync to Hugging Face Spaces

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - 'backend/**'

jobs:
  sync-to-hub:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Deploy Backend to Hugging Face
        env:
          HF_TOKEN: ${{ secrets.HF_TOKEN }}
        run: |
          git config --global user.email "harigodgamer@gmail.com"
          git config --global user.name "HariGodGamer"
          git subtree split --prefix backend -b hf-deploy
          git push -f https://HariGodGamer:${HF_TOKEN}@huggingface.co/spaces/HariGodGamer/neuroassit hf-deploy:main
